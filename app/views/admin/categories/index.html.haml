%h1 Categories
= link_to 'New', new_admin_category_path, class: 'btn btn-primary'
- if @categories.present?
  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Title
        %th Number of Products
    %tbody
      - @categories.each do |category|
        %tr
          %td= link_to category.name, admin_category_path(category)
          %td= category.title
          %td= category.products.count
          %td
            = link_to 'Edit', edit_admin_category_path(category), class: 'btn btn-mini'
            = link_to 'Destroy', admin_category_path(category), method: :delete, confirm: 'Are you sure?', class: 'btn btn-mini btn-danger'
- else
  .well.text-center No Categories were created yet.
